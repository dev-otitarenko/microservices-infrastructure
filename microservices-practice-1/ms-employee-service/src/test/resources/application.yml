# Spring conf
spring:
  application:
    name: dept-service
  datasource:
    url: jdbc:h2:mem:DBTestEmp;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop